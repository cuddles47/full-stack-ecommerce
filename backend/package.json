{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend server for the application",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/server.ts",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "start": "node dist/server.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "seed": "ts-node ./src/scripts/setup.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.3",
    "express": "^5.1.0",
    "multer": "^1.4.5-lts.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/multer": "^1.4.12",
    "nodemon": "^3.1.10",
    "ts-node-dev": "^2.0.0"
  }
}
